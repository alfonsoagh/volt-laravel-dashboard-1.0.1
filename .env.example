IS_DEMO=false

APP_NAME=Laravel
APP_ENV=local
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

LOG_CHANNEL=stack
LOG_LEVEL=debug

DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=
DB_USERNAME=
DB_PASSWORD=

BROADCAST_DRIVER=log
CACHE_DRIVER=file
QUEUE_CONNECTION=sync
SESSION_DRIVER=file
SESSION_LIFETIME=120

MEMCACHED_HOST=127.0.0.1

REDIS_HOST=127.0.0.1
REDIS_PASSWORD=null
REDIS_PORT=6379

MAIL_MAILER=smtp
MAIL_HOST=smtp.mailtrap.io
MAIL_PORT=2525
MAIL_USERNAME=
MAIL_PASSWORD=
MAIL_ENCRYPTION=tls

IS_DEMO=false

# ================= CETAM Project Overrides =================
# Prefija las variables clave con CETAM_PROJ_ para personalización por proyecto.
# Sustituye "PROJ" por el código real del proyecto si lo deseas.

# Slug para prefijo de rutas /p/<slug>
CETAM_PROJ_SLUG=proj
# Prefijo de nombres de ruta (por convención "proj")
CETAM_PROJ_ROUTE_NAME_PREFIX=proj

# (Opcional) Alias de variables comunes del proyecto
CETAM_PROJ_APP_NAME=${APP_NAME}
CETAM_PROJ_APP_URL=${APP_URL}
CETAM_PROJ_DB_HOST=${DB_HOST}
CETAM_PROJ_DB_PORT=${DB_PORT}
CETAM_PROJ_DB_DATABASE=${DB_DATABASE}
CETAM_PROJ_DB_USERNAME=${DB_USERNAME}
CETAM_PROJ_DB_PASSWORD=${DB_PASSWORD}
